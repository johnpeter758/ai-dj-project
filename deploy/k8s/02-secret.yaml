apiVersion: v1
kind: Secret
metadata:
  name: ai-dj-secret
  namespace: ai-dj
type: Opaque
stringData:
  # Add your secrets here (use kubectl create secret generic to generate)
  # Example:
  # API_KEY: "your-api-key-here"
  # DATABASE_URL: "postgresql://user:pass@host:5432/db"
  # REDIS_PASSWORD: "your-redis-password"
